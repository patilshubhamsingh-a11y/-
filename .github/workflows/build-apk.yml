name: build
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 30
      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Restore and Fix
        run: |
          git checkout 1b8b059 -- app/src/main/java || git checkout HEAD~4 -- app/src/main/java || echo "no restore"
          cat > app/build.gradle.kts <<'GRADLE'
          plugins {
              alias(libs.plugins.android.application)
              alias(libs.plugins.kotlin.android)
          }
          android {
              namespace = "com.example.gajananmandir"
              compileSdk = 34
              defaultConfig {
                  applicationId = "com.example.gajananmandir"
                  minSdk = 24
                  targetSdk = 34
                  versionCode = 1
                  versionName = "1.0"
              }
              buildTypes { release { isMinifyEnabled = false } }
              compileOptions {
                  sourceCompatibility = JavaVersion.VERSION_17
                  targetCompatibility = JavaVersion.VERSION_17
              }
              kotlinOptions { jvmTarget = "17" }
              buildFeatures { compose = true }
              composeOptions { kotlinCompilerExtensionVersion = "1.5.8" }
          }
          dependencies {
              implementation(libs.androidx.core.ktx)
              implementation(libs.androidx.appcompat)
              implementation(libs.material)
              implementation(libs.androidx.activity.compose)
              implementation(platform(libs.androidx.compose.bom))
              implementation(libs.androidx.compose.ui)
              implementation(libs.androidx.compose.material3)
              implementation("androidx.compose.material:material-icons-extended")
              implementation("io.coil-kt:coil-compose:2.5.0")
              implementation("androidx.compose.foundation:foundation")
          }
          GRADLE
          cat > /tmp/fix.py <<'PY'
          import os
          base="app/src/main/java"
          for root,_,files in os.walk(base):
            for fn in files:
              if not fn.endswith(".kt"): continue
              p=os.path.join(root,fn)
              txt=open(p,'r',errors='ignore').read()
              orig=txt
              if "RoundedCornerShape" in txt and "foundation.shape" not in txt:
                txt=txt.replace("import androidx.compose.material3", "import androidx.compose.foundation.shape.RoundedCornerShape\nimport androidx.compose.material3")
              if "AsyncImage" in txt and "coil.compose.AsyncImage" not in txt:
                txt="import coil.compose.AsyncImage\n"+txt
              if "Icons.Filled" in txt and "import androidx.compose.material.icons.Icons" not in txt:
                txt="import androidx.compose.material.icons.Icons\nimport androidx.compose.material.icons.filled.*\n"+txt
              if "R.drawable" in txt and "com.example.gajananmandir.R" not in txt:
                if "package com.example" in txt:
                  txt=txt.replace("package com.example", "import com.example.gajananmandir.R\npackage com.example",1)
              if "outlinedButtonBorder()" in txt:
                txt=txt.replace("outlinedButtonBorder()", "androidx.compose.foundation.BorderStroke(1.dp, androidx
